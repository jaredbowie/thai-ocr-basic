(ns thaiocr.test555
  (:require [thaiocr.test555 :refer :all]
            [clojure.test :refer :all]))

(defn play-with []
  (let [some-map {:keya {:keyb {:keyc {:keyd 5555}}}}]
    (flatten some-map)
    )
  )
